# Backend - KV Storage Data Files

Тази папка съдържа JSON файловете, които трябва да се качат в Cloudflare KV storage за правилното функциониране на ОТСЛАБВАНЕ сайта.

## 📁 Структура на Файловете

```
backend/
├── page_content.json    # Пълното съдържание на сайта (използва се от index.html)
├── site_content.json    # Алтернативно име за същото съдържание
├── products.json        # Само продуктите (за лесен достъп)
├── orders.json          # Масив с поръчки (започва празен)
└── README.md           # Тази документация
```

## 🎯 Предназначение на Файловете

### 1. `page_content.json` (Главен файл)
Това е основният файл, който се използва от frontend-а. Съдържа:
- **settings**: Име на сайта, слоган, лого
- **navigation**: Менюта за навигация
- **page_content**: Всички компоненти на страницата (hero, продукти, FAQ, и т.н.)
- **footer**: Футър информация и линкове

**Използване**: Този файл трябва да се качи в KV под ключа `page_content`.

### 2. `site_content.json`
Копие на `page_content.json` за резервно съхранение или алтернативно използване.

### 3. `products.json`
Извлечен списък само с продукти от `page_content.json` в опростен формат:
```json
{
  "products": [
    {
      "product_id": "...",
      "public_data": { ... },
      "system_data": { ... }
    }
  ]
}
```

**Използване**: Може да се използва за по-лесен достъп до продуктите от admin панела или за AI препоръки.

### 4. `orders.json`
Празен масив `[]` в начален вид. Ще се попълва автоматично с поръчки от клиенти през:
- Въпросника (quest.html)
- Checkout формата
- Admin панела

## 🚀 Как да Качите Файловете в KV

### Метод 1: Използване на Wrangler CLI (Препоръчително)

```bash
# 1. Инсталирайте wrangler
npm install -g wrangler

# 2. Влезте в Cloudflare акаунта си
wrangler login

# 3. Качете главния файл
wrangler kv:key put --namespace-id="d220db696e414b7cb3da2b19abd53d0f" \
  "page_content" \
  --path="backend/page_content.json"

# 4. Качете orders (ако е необходимо)
wrangler kv:key put --namespace-id="d220db696e414b7cb3da2b19abd53d0f" \
  "orders" \
  --path="backend/orders.json"
```

**Забележка**: `d220db696e414b7cb3da2b19abd53d0f` е ID-то на PAGE_CONTENT namespace от wrangler.toml

### Метод 2: През Cloudflare Dashboard (GUI)

1. Влезте в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Отидете на **Workers & Pages** → **KV**
3. Изберете namespace **PAGE_CONTENT**
4. Кликнете **Add entry**
5. За Key използвайте: `page_content`
6. Копирайте съдържанието на `backend/page_content.json` и го поставете като Value
7. Кликнете **Add**

### Метод 3: През API Endpoint

```bash
# Използвайте worker API endpoint
curl -X POST https://your-worker.workers.dev/page_content.json \
  -H "Content-Type: application/json" \
  --data @backend/page_content.json
```

## ✅ Верификация

След качването, проверете дали съдържанието е правилно:

### 1. Проверка през Browser
Отворете: `https://your-worker.workers.dev/page_content.json`

Трябва да видите:
```json
{
  "settings": {
    "site_name": "ОТСЛАБВАНЕ",
    "site_slogan": "Ефективни решения за здравословен живот",
    ...
  }
}
```

### 2. Проверка през Verification Tool
Отворете `test-content.html` във вашия browser. Ще видите:
- ✅ **Зелен статус** - Съдържанието е коректно
- ❌ **Червен статус** - Нещо не е наред

### 3. Проверка на Main Site
Отворете `index.html` - трябва да видите само ОТСЛАБВАНЕ брандинг без смяна на съдържание.

## 🔄 Актуализация на Съдържанието

Когато искате да промените съдържанието:

1. **Редактирайте локално**:
   ```bash
   # Редактирайте файла
   nano backend/page_content.json
   
   # Или използвайте admin панела
   open admin.html
   ```

2. **Качете промените**:
   ```bash
   # Wrangler метод
   wrangler kv:key put --namespace-id="d220db696e414b7cb3da2b19abd53d0f" \
     "page_content" \
     --path="backend/page_content.json"
   
   # Или през API
   curl -X POST https://your-worker.workers.dev/page_content.json \
     -H "Content-Type: application/json" \
     --data @backend/page_content.json
   ```

3. **Проверете промените**:
   - Изчистете browser cache (Ctrl+Shift+Delete)
   - Презаредете сайта
   - Проверете с test-content.html

## 📊 Структура на Данните

### Settings Object
```json
{
  "site_name": "ОТСЛАБВАНЕ",
  "site_slogan": "Ефективни решения за здравословен живот",
  "logo_url": ""
}
```

### Navigation Array
```json
[
  {
    "text": "За нас",
    "link": "#benefits"
  }
]
```

### Page Content Components

Поддържани типове компоненти:
- `hero_banner` - Hero секция с canvas анимация
- `product_category` - Категория с продукти
- `info_card` - Информационна карта с картинка
- `benefits` - Секция с предимства
- `timeline` - Стъпка по стъпка процес
- `ingredients` - Съставки с flip cards
- `testimonials` - Отзиви от клиенти
- `faq` - Често задавани въпроси
- `guarantee` - Гаранции и сигурност
- `contact` - Контактна форма

### Product Object
```json
{
  "product_id": "unique-id",
  "public_data": {
    "name": "Име на продукта",
    "tagline": "Кратко описание",
    "price": 69.99,
    "description": "Пълно описание...",
    "image_url": "url/to/image",
    "effects": [
      {
        "label": "Ефект",
        "value": 85
      }
    ]
  },
  "system_data": {
    "inventory": 50
  }
}
```

## 🛠️ Troubleshooting

### Проблем: Все още виждам BIOCODE съдържание

**Решение**:
1. Проверете дали сте качили правилния файл
2. Изчистете browser cache
3. Проверете с: `curl https://your-worker.workers.dev/page_content.json`
4. Ако все още показва BIOCODE, качете отново файла

### Проблем: 404 грешка при достъп до /page_content.json

**Решение**:
1. Проверете дали worker-ът е деплойнат
2. Проверете дали KV binding е конфигуриран в wrangler.toml
3. Проверете дали ключът 'page_content' съществува в KV

### Проблем: Продуктите не се показват

**Решение**:
1. Проверете JSON структурата на продуктите
2. Уверете се че всеки продукт има `product_id` и `public_data`
3. Проверете console за грешки (F12 в browser)

## 📝 Важни Забележки

1. **Backup**: Винаги правете backup на текущото съдържание преди да го презаписвате
2. **Валидация**: Винаги валидирайте JSON преди качване (използвайте jsonlint.com)
3. **Тестване**: Тествайте промените локално преди да качите в production
4. **Version Control**: Commit-вайте промените в git за history

## 🎯 Следващи Стъпки

След качване на файловете:

1. ✅ Качете `page_content.json` в KV
2. ✅ Проверете с test-content.html
3. ✅ Тествайте основния сайт (index.html)
4. ✅ Проверете че няма FOUC (flash of unstyled content)
5. ✅ Тествайте всички продуктови страници
6. ✅ Проверете че въпросникът работи (quest.html)

## 🆘 Помощ

За допълнителна помощ:
- Вижте [../SOLUTION-SUMMARY.md](../SOLUTION-SUMMARY.md)
- Вижте [../docs/FIX-CONTENT-MISMATCH.md](../docs/FIX-CONTENT-MISMATCH.md)
- Вижте [../docs/ІНСТРУКЦІЇ-ПОТРЕБИТЕЛ.md](../docs/ІНСТРУКЦІЇ-ПОТРЕБИТЕЛ.md)

---

**Последна актуализация**: Декември 2025  
**Версия**: 1.0 - ОТСЛАБВАНЕ съдържание
