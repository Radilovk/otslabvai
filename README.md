# ОТСЛАБВАНЕ - Онлайн Магазин за Продукти за Отслабване

Уеб приложение за продукти за отслабване и контрол на теглото. Продуктите се визуализират чрез ключа `page_content` в Cloudflare KV (достъпно през `/page_content.json`) и поръчките се съхраняват в Cloudflare KV.

## 🎯 Актуална Информация

⚠️ **Важно**: Наскоро бе поправен проблем с препокриваща се информация в JSON файловете. Моля, прочетете:
- **[SOLUTION_SUMMARY.md](SOLUTION_SUMMARY.md)** - Пълно описание на решението
- **[FIX_DEPLOYMENT.md](FIX_DEPLOYMENT.md)** - Инструкции за деплой
- **[backend/STRUCTURE_EXPLANATION.md](backend/STRUCTURE_EXPLANATION.md)** - Структура на backend файловете

## 📦 Продукти (18 общо)

Магазинът предлага 5 категории продукти:
- **Изгаряне на мазнини** - 5 продукта (RAW Nutrition, Trec, AllNutrition, EthicSport, Sport Definition)
- **Контрол на апетита** - 3 продукта (Глюкоманан, 5-HTP, Хром пиколинат)
- **Ускоряване на метаболизма** - 3 продукта (Кофеин, Витамин B, Капсаицин)
- **Детокс и пречистване** - 3 продукта (Хлорела, Бял трън, Пробиотици)
- **Продукти за отслабване** - 4 продукта (MeiziMax, Lida Green, Eveslim Cayenne, Eveslim Birch)

## Архитектура на проекта

### Backend JSON файлове
* **backend/page_content.json** – PRIMARY файл, зареждан от приложението (18 продукта)
* **backend/products.json** – Референтен файл за продукти (не се използва директно)
* **backend/site_content.json** – Референтен файл за site структура (не се използва директно)
* **page_content_mock.json** – Локално fallback копие на page_content.json

### Worker и код
* **worker.js** – Cloudflare Worker, деплойва се чрез `wrangler deploy`
* **index.js** – Frontend логика за зареждане и рендиране на продукти
* **product.js** – Логика за детайлни страници на продукти
* **admin.js** – Админ панел за управление на съдържание
* **config.js** – API конфигурация

## Стартиране и Деплой

### Локално тестване
```bash
# 1. Инсталирайте зависимостите
npm install

# 2. Валидирайте backend JSON файловете
npm run validate-backend

# 3. Стартирайте локален worker
wrangler dev
# или
npm start
```

### Деплой в продукция
```bash
# 1. Качете статичните файлове в KV
npm run upload-static

# 2. Качете page_content.json в KV
cd backend
wrangler kv:key put --binding=PAGE_CONTENT page_content --path=page_content.json

# 3. Деплойнете worker-a
npm run deploy
```

За подробни инструкции вижте [FIX_DEPLOYMENT.md](FIX_DEPLOYMENT.md).

## Конфигурация на API адреса
Файлът **config.js** съдържа адреса на бекенд API-то, използван от клиентските скриптове.
За да го промените, редактирайте стойността на `API_URL` в този файл:

```javascript
export const API_URL = 'https://example.com';
```

При деплой в различна среда заменете или генерирайте съответния `config.js`.

## Страници
- **index.html** – основна витрина с категории продукти. Зарежда данни от `/page_content.json` (KV key: `page_content`)
- **product.html** – детайлна страница на продукт
- **admin.html** – админ панел за редактиране на съдържание
- **checkout.html** – процес на поръчка
- **quest.html** – въпросник за персонализирани препоръки

## Как да актуализирам продукти?

1. **Редактирайте** `backend/page_content.json`
2. **Валидирайте**: `npm run validate-backend`
3. **Синхронизирайте mock**: `cp backend/page_content.json page_content_mock.json`
4. **Качете в KV**:
   ```bash
   wrangler kv:key put --binding=PAGE_CONTENT page_content --path=backend/page_content.json
   ```

Вижте [backend/STRUCTURE_EXPLANATION.md](backend/STRUCTURE_EXPLANATION.md) за подробности.

## Документация

- **[SOLUTION_SUMMARY.md](SOLUTION_SUMMARY.md)** - Пълно описание на последната поправка
- **[FIX_DEPLOYMENT.md](FIX_DEPLOYMENT.md)** - Инструкции за деплой
- **[backend/STRUCTURE_EXPLANATION.md](backend/STRUCTURE_EXPLANATION.md)** - Структура на backend файлове
- **[BACKEND_UPDATE_SUMMARY.md](BACKEND_UPDATE_SUMMARY.md)** - История на промени
- **[HOW_TO_FIX.md](HOW_TO_FIX.md)** - Общи инструкции за поправки

## Scripts

```bash
npm start                # Стартира локален worker (wrangler dev)
npm run deploy          # Деплойва worker в продукция
npm run upload-static   # Качва статични файлове в KV
npm run validate-backend # Валидира backend JSON файлове
npm test                # Стартира тестове (ако има)
```
- **checkout.html** – страница за завършване на поръчката. Изпраща кошницата към `orders`. При хостване в поддиректория използвайте относителния път.
- **admin.html** – администраторски панел, също организиран в три секции (хедър, табове за съдържание и шаблони). Отваряйте го през HTTP, напр. `http://localhost:3000/admin.html`.
- *От версия 1.1* страниците разполагат с подобрен responsive дизайн за телефони.
- **page_content.json** – JSON съдържание, достъпно през този маршрут и пазено в KV под ключ `page_content`.
- **orders.json** пази направените поръчки при локално стартиране.
## Cloudflare Workers
Cloudflare Workers е безсървърна среда и се различава от Node изпълнението на `local/local-worker.js`.
Вместо Express и локална файлова система се използват `fetch` събития и KV хранилища.

Файлът `wrangler.toml` е част от проекта и се следи от Git. В него са зададени имената на KV пространствата и други настройки за Worker-а. Ако използвате собствен акаунт, редактирайте този файл, вместо да създавате нов.

### Локално тестване
- Стартирайте `local/local-worker.js` с:
  ```bash
  node local/local-worker.js
  ```
  Това осигурява локално API на порт **3000**.

### Копиране в Cloudflare Dashboard
1. Влезте в **Dashboard** и изберете **Workers**.
2. Създайте нов Worker и копирайте съдържанието на `worker.js` в онлайн редактора. Файлът вече съдържа цялата логика и не изисква файла `local/handler.js`.
3. Запишете и тествайте функцията.

### KV пространства
1. Създайте `ORDERS` и `PAGE_CONTENT` с:
   ```bash
   wrangler kv:namespace create ORDERS
   wrangler kv:namespace create PAGE_CONTENT
   ```
   (или през Dashboard > Workers > KV)
2. Добавете binding към worker-а в `wrangler.toml` или чрез Dashboard:
   ```toml
   [[kv_namespaces]]
   binding = "ORDERS"
   id = "<id>"
   preview_id = "<id>"

  [[kv_namespaces]]
  binding = "PAGE_CONTENT"
  id = "<id>"
  preview_id = "<id>"
  ```

## Работа с KV
При стартиране на `node local/local-worker.js` данните се записват във файловете
`orders.json` и `page_content.json`. Това е удобно за бързи тестове без Cloudflare.

Когато използвате `wrangler dev` или пуснете worker-а онлайн, поръчките и
съдържанието се съхраняват в KV пространствата `ORDERS` и `PAGE_CONTENT`.

### Проверка през Dashboard
1. Влезте в **Cloudflare Dashboard**.
2. Изберете **Workers & KV** и отворете желаното пространство.
3. В раздел **Browse** ще видите записите и можете да ги редактирате.

Помислете за автоматично архивиране на KV, ако проектът се разрасне.

### Деплой на `worker.js`
1. Инсталирайте `wrangler`.
2. Попълнете идентификаторите в `wrangler.toml`.
3. Публикувайте с:
   ```bash
   wrangler deploy
   ```
4. Скриптът може и да се качи ръчно през Cloudflare Dashboard – просто копирайте съдържанието на файла.
5. След преминаване към `worker.js` можете да премахнете или архивирате `local/local-worker.js`.

## Локални данни
В папка `kv` са добавени JSON файлове, които представляват примерни стойности
за Cloudflare KV. Използвайте ги само за локални тестове.

## Стари демо страници
В папка `src/` ще намерите по-ранни примери за клиентска и администраторска страница със смяна на тема и локално задаване на цена. Могат да се използват за справка или да бъдат премахнати при нужда.

## Принос
Изпращайте промени чрез Pull Request. Лицензът е MIT.
