# Нови функции в Admin панела

## 1. Редактиране на Hero секция - Пълна контрол над текстове

### Статистики (Stats)
Всички статистики в hero секцията вече са напълно редактируеми:
- **Стойност** - Числото или процентът който се показва (напр. "2,840+" или "98%")
- **Етикет** - Текстът под стойността (напр. "Доволни клиенти")

**Как да редактирате:**
1. Отворете Admin панела
2. Отидете на табa "Съдържание"
3. Кликнете "Редактирай" на Hero Banner
4. Превъртете до секцията "📊 Статистики"
5. Редактирайте съществуващите статистики или добавете нови с бутона "Добави статистика"
6. Всяка статистика може да се изтрие с бутона "×"

### Trust Badges (Доверителни знаци)
Trust badges също са напълно редактируеми:
- **Текст на badge** - Текстът който се показва в badge (напр. "Клинично тествано")

**Как да редактирате:**
1. В същия Hero Banner редактор
2. Превъртете до секцията "🛡️ Trust Badges"
3. Редактирайте текста на всеки badge или добавете нови
4. Всеки badge може да се изтрие с бутона "×"

## 2. Upload на изображения за продукти

### Функционалност
Сега можете директно да качвате изображения в репото вместо да въвеждате URL адреси.

### Как да качите изображение:
1. Отворете Admin панела
2. Отидете на "Съдържание" таб
3. Редактирайте продуктова категория или добавете нова
4. В продуктовия редактор, намерете полето "Основно изображение (URL)"
5. Кликнете бутона "📤 Upload" до полето
6. Изберете изображение от вашия компютър
7. При първо качване ще бъдете помолени да въведете GitHub Personal Access Token
8. След успешно качване, URL-ът ще бъде автоматично попълнен

### Ограничения
- **Тип файл**: Само изображения (JPG, PNG, GIF, WebP и др.)
- **Размер**: Максимум 2MB
- **Формат на име**: Автоматично се генерира уникално име с timestamp

### GitHub Personal Access Token

За да качвате изображения, трябва да имате GitHub Personal Access Token с `repo` permissions:

1. Отидете на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Кликнете "Generate new token (classic)"
3. Дайте име на token-а (напр. "otslabvai-image-upload")
4. Изберете scope: `repo` (пълен достъп до private repositories)
5. Генерирайте token-а и копирайте го
6. **ВАЖНО**: Запазете token-а на сигурно място - не може да бъде видян отново

**Сигурност**: Token-ът се съхранява в sessionStorage и се изтрива когато затворите браузъра. Ако token-ът е невалиден, автоматично се изтрива и ще бъдете помолени да въведете нов.

### Къде се качват изображенията?
Изображенията се качват в: `images/products/` директория в репото.

Пример URL: `https://raw.githubusercontent.com/Radilovk/otslabvai/main/images/products/product-1234567890-image.jpg`

## Технически детайли

### Hero секция промени
- **Файлове променени**: 
  - `admin.html` - Добавени templates за stats и trust_badges
  - `admin.js` - Добавена логика за сериализация и популация
  - `index.js` - Актуализиран rendering за динамични trust badges
  - `backend/page_content.json` - Добавени trust_badges данни

### Image upload промени
- **Файлове променени**:
  - `admin.html` - Добавен upload бутон и скрит file input
  - `admin.js` - Добавена `uploadImageToGitHub()` функция и event handler
  - `images/products/` - Нова директория за продуктови изображения

### API използване
Upload функцията използва GitHub Contents API:
- Endpoint: `PUT /repos/{owner}/{repo}/contents/{path}`
- Authentication: Personal Access Token
- Content encoding: Base64

## Известни ограничения

1. **GitHub Token**: Token-ът се съхранява в sessionStorage (валиден до затваряне на браузъра)
2. **Размер на файл**: GitHub има лимит от 100MB за файл, но нашата валидация е 2MB за оптимална производителност
3. **Име на файл**: Автоматично се генерира и не може да се променя (за уникалност)
4. **Authentication**: Използва се Bearer token authentication (GitHub API v3 стандарт)

## 3. Автоматична конфигурация на GitHub API Token

### Функционалност
Вместо да въвеждате GitHub token всеки път, сега можете да го съхраните в KV и той ще се използва автоматично.

### Как работи:
1. GitHub API токенът се съхранява в Cloudflare KV с ключ `api_token`
2. При опит за качване на изображение, админ панелът автоматично извлича токена от backend
3. Токенът се кешира в sessionStorage за по-бърз достъп
4. Ако токенът не е конфигуриран, все още можете да го въведете ръчно

### Как да конфигурирате токена:
```bash
./set-api-token.sh
```

Или директно с wrangler:
```bash
echo "your_github_token" | wrangler kv:key put --binding="PAGE_CONTENT" "api_token" --path=-
```

**Вижте `API_TOKEN_CONFIG.md` за пълна документация.**

## Бъдещи подобрения

- [x] Запазване на GitHub token в sessionStorage (безопасно за сесията)
- [x] Използване на Bearer token authentication
- [x] Автоматично изтриване на невалиден token
- [x] Съхранение на GitHub token в KV за автоматично използване
- [ ] Batch upload за множество изображения
- [ ] Преглед на качените изображения
- [ ] Изтриване на стари изображения
- [ ] Оптимизация и компресия на изображения преди upload
- [ ] Upload на допълнителни изображения (не само основното)
